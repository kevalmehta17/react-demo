import React from 'react'
// Reusable field component for form inputs, dropdowns, etc. Can be extended as needed.
const Field = ({label, value, onChange}) => {
  return (
    <div>
      <label>{label}</label>
      <select value={} ></select>
    </div>
  )
}

export default Field
